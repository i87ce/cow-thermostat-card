# Add the following to your Home Assistant configuration.yaml.
# After editing, restart Home Assistant (or "Reload Lovelace resources"
# under Settings -> Server controls if only the Lovelace block changed).

# 1. Register the custom card resource (HACS does this for you in storage
#    mode; only needed if you are in YAML-only mode).
frontend:
  extra_module_url:
    - /hacsfiles/cow-thermostat-card/cow-thermostat-card.js
    # Required for the ?kiosk URL parameter to hide HA header + sidebar
    - /hacsfiles/kiosk-mode/kiosk-mode.js

# 2. Register one dashboard per Wall Display.
#    These do NOT show up in the sidebar — they exist only at their direct URL.
lovelace:
  mode: storage  # leave default; per-dashboard "mode: yaml" lets us version the YAML
  dashboards:
    walldisplay-living:
      mode: yaml
      filename: dashboards/walldisplay-living.yaml
      title: Sala
      show_in_sidebar: false
      require_admin: false
    walldisplay-cucina:
      mode: yaml
      filename: dashboards/walldisplay-cucina.yaml
      title: Cucina
      show_in_sidebar: false
      require_admin: false
    walldisplay-camera-padronale:
      mode: yaml
      filename: dashboards/walldisplay-camera-padronale.yaml
      title: Camera Padronale
      show_in_sidebar: false
      require_admin: false
    walldisplay-camera-1:
      mode: yaml
      filename: dashboards/walldisplay-camera-1.yaml
      title: Camera 1
      show_in_sidebar: false
      require_admin: false
    walldisplay-camera-2:
      mode: yaml
      filename: dashboards/walldisplay-camera-2.yaml
      title: Camera 2
      show_in_sidebar: false
      require_admin: false
    walldisplay-studio:
      mode: yaml
      filename: dashboards/walldisplay-studio.yaml
      title: Studio
      show_in_sidebar: false
      require_admin: false
